# mainmenu alias replaces these frames

aliases:

  transitions:
# an collection of frames that can be used elsewhere in the config
# to avoid rewriting frames
    cleartomain:
      - type: "item"
        contents:
          material: "honeycomb"
          displayname: "UMBCraft"
          id: "lol"
          slot: 12
          delay: 1
      - type: "item"
        contents:
          material: "redstone_dust"
          displayname: "Settings"
          id: "to_settings"
          slot: 0
          delay: 0
          count: 3